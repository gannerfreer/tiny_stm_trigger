# TODO（业务逻辑拆解）

## 目标说明
- 核心逻辑：当任一核心输入异常（内部温度越界 / 数字输入触发）时，调整 PWM 输出脉宽并锁存。
- 启动保护：上电/复位后的前 `15s` 内，**禁止触发**上述输出逻辑。
- 反馈采样：通过 `AD_T1` / `AD_T2` 采集电压反馈，计算输入电压供控制逻辑使用。
- PWM 策略：
  - `PWM0`：基于 `AD_T1` 反馈闭环调节，目标电压 `1.5V`。
  - `PWM1` / `PWM2`：触发后脉宽拉满 `100%`，未触发为 `0%`。
  - 基础频率：`10kHz`。
- 开发优先级：先完成核心逻辑，再补充 I2C、LED、按键、串口指示灯等辅助功能。

## 核心功能 I/O 定义（按代码 Tag）
- 输出（PWM）：
  - `PWM0`
  - `PWM1`
  - `PWM2`
- 输入（数字）：
  - `OPEN`（水传感器常开）
  - `CLOSE`（水传感器常闭）
  - `GAS_D1`
  - `GAS_D2`
- 输入（模拟）：
  - `ADC 内部温度`（内部温度通道）
  - `AD_T1`（电压反馈）
  - `AD_T2`（电压反馈）
- 数字输入策略：
  - `OPEN` / `CLOSE` / `GAS_D1` / `GAS_D2` 使用 `GPIO_MODE_INPUT`，在主循环周期读取引脚电平，不使用 EXTI 中断。
  - 原因：实时性要求不高，但需要稳定性（可在软件层增加连续采样判稳/去抖）。

## 阶段1：核心逻辑（优先）
- [x] 定义阈值参数：`TEMP_MIN/TEMP_MAX`（当前已用宏占位，后续按实测替换）。
- [x] 定义 `AD_T1` / `AD_T2` 电压换算与输入电压计算公式（分压、参考电压、校准系数，当前系数为占位）。
- [x] 定义 `PWM0` 闭环调节策略（目标 `1.5V`、控制步进/限幅、触发后锁存策略）。
- [x] 建立启动计时：使用 `HAL_GetTick()` 生成 `boot_guard_active = (tick < 15000)`。
- [x] 完成 ADC 采样通路：`AD_T1` / `AD_T2` 与内部温度，周期获取最新值。
- [x] 完成数字输入采集：主循环读取 `OPEN` / `CLOSE` / `GAS_D1` / `GAS_D2`，并加入基础判稳（N 次一致才生效）。
- [x] 新增统一判定函数（`CoreProtection_Update()`）：
  - [x] 若 `boot_guard_active == true`，则不允许触发 PWM 输出变化。
  - [x] 若 `boot_guard_active == false` 且任一条件满足（温度越界/数字输入触发），则调整 PWM 脉宽并锁存。
  - [x] 基于 `AD_T1` / `AD_T2` 计算输入电压，用于 PWM 脉宽计算。
- [x] 明确 PWM 输出行为：触发后锁存到复位，不做自动切换。
- [x] 在主循环中固定周期执行判定，避免抖动。

## 阶段2：验证与稳定性
- [ ] 用串口打印关键状态：`tick`、`AD_T1/AD_T2`、温度值、`OPEN/CLOSE/GAS_D1/GAS_D2`、PWM 占空比/输出状态。
- [ ] 编写最小化测试清单：
  - [ ] 0~15s 内制造异常，确认 PWM 输出不变化。
  - [ ] >15s 后分别触发温度越界、`OPEN/CLOSE/GAS_D1/GAS_D2`，确认 PWM 脉宽调整并锁存。
  - [ ] 边界值测试（刚好 15000ms、阈值上下边界、PWM 脉宽上下限）。
  - [ ] `AD_T1/AD_T2` 电压换算与输入电压计算正确性测试。
  - [ ] `PWM0` 闭环目标 `1.5V` 稳定性测试（负载变化/输入波动下的稳态误差）。
- [ ] 若出现误触发，加入滤波/连续超限计数策略。

## 阶段3：辅助功能（核心完成后）
- [ ] I2C 外设接入与通信自检。
- [x] LED 状态指示：
  - [x] LED0：系统状态，500ms 翻转一次（心跳）。
  - [x] LED1：触发指示，触发后 100ms 翻转快闪（锁存到复位）。
- [ ] 按键功能（状态清除、模式切换或调试触发）。
- [x] 串口日志增强（便于联调）：
  - [x] 独立串口辅助文件封装发送函数。
  - [x] 周期打印核心状态（保护、输入采样、故障标志、PWM 状态）。

## 待确认问题
- [x] PWM 触发后锁存到复位。
- [ ] 内部温度“超过一定范围”是上限、下限还是双向越界？
- [ ] `OPEN` / `CLOSE` / `GAS_D1` / `GAS_D2` 的触发电平定义（高有效/低有效）与判稳窗口长度。
- [ ] `AD_T1` / `AD_T2` 反馈电压的换算关系（分压比/参考电压/校准系数）。
